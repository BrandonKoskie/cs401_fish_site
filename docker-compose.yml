version: '3.8'

services:
  flask-app:
    build: .
    container_name: hawaii-seafood-app
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app
    restart: unless-stopped